#include <stdio.h>

void comparaison_points(int ax, int ay, int bx, int by, int cx, int cy, int dx, int dy) {
    // Préconditions: ax, ay, bx, by, cx, cy, dx, dy sont des entiers positifs

    // TODO: comparer les points et afficher s'ils sont confondus ou si les droites sont parallèles ou si aucun des deux

    // Postconditions: la fonction affiche si les points sont confondus ou si les droites sont parallèles ou s'il n'y a aucun points confondus ni aucune droite parallèle
}

// Ne pas modifier la ligne suivante
#ifndef TEST_IHDCB131

int main(void) {
    int ax, ay, bx, by, cx, cy, dx, dy;

    // TODO: afficher une phrase demandant d'encoder les coordonnées des points A, B, C et D et récupérer les valeurs

    // ----------------------------------
    // NE PAS MODIFIER LE CODE CI-DESSOUS
    // ----------------------------------

    // Appel de la fonction comparaison_points qui compare les points et affiche s'ils sont confondus ou si les droites AB et CD sont parallèles
    comparaison_points(ax, ay, bx, by, cx, cy, dx, dy);

    return 0;
}

// Ne pas modifier la ligne suivante
#endif